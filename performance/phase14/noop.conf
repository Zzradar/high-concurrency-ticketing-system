events { worker_connections 4096; }
http {
    access_log off;
    server {
        listen 8080;
        location = /health {
            default_type application/json;
            return 200 '{"database":"up","status":"ok"}';
        }
    }
}
