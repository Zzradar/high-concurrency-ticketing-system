# Phase18 hardening and full regression evidence

Capability evidence, separate from formal OFF A/B. Source is bound by commit and Git blob hashes in manifest.json; raw logs retain original bytes. Runtime fixtures are named Phase18 containers only. Source default pools and protocol did not change: 4 PG connections, original Redis pools 2+2 plus the designed independent traffic pool 2.

Gates: CTest 36/36; frontend Vitest 288/288 and production real API build; performance Python 242/242. Complete OFF regression: Phase15 Reservation 4, Checkout 7, read 2, observation 2; Phase16 migration 5, Redis/read model 11, HTTP 7; Phase17 Venue 4 and Publish 9; simulation payment 6. Separate schema: Phase15 4, Phase17 8, Phase18 2. Original Phase17 Auth/Zone 5/5 ran on a separate test-only probe build; that probe is excluded from the formal Release candidate.

Policy HTTP 7/7, Admission HTTP 5/5, Layout HTTP 3/3, waiting Lua 7/7, token Lua 3/3. Real PostgreSQL delay with inventory limit 1: peak 1, surplus 503, timed-out request 500, zero residual checkout, two seats available, permit drained 0, event cooldown prevented release then resumed; financial/recovery 200. This limit change was capability-only and restored, never an A/B setting.

16 users x 8 events, 128 joins: Phase18 metric series count [31,31,31,31,31,31,31,31], no identity labels. Two real APIs/schedulers using the same generated HMAC configuration: 16 duplicate joins -> one position, active limit 1 never exceeded during five successive releases. PG pool budget for this test was 8. The separate Fake Stripe capability used 4 HTTP threads per API to bound fixture overhead; formal A/B retains the baseline source thread setting and exact cgroup limits.

Actual Checkout exit 88 after formal commit: persisted SUBMITTING, sales ended, repeated confirm 200 with same single order. Availability projector exit 86 and initializer exit 87 both recover without duplicate changes; Redis outage keeps authoritative PG snapshot/outbox, namespace reset changes generation, PG rollback preserves stock. Cold 5000-seat model: 48 requests/concurrency 8, one full SQL initializer. Concurrency is below the newly intentional local limit 16; this capability does not claim all-200 at the old 24-concurrency fault fixture and does not modify the frozen A/B protocol.

Unchanged original Fake Stripe suites on an exclusive Phase18 Compose project: Phase11 Stripe 11/11; payment/reconciliation crash 11/11; Phase12 buyer refund 38/38, including actual process kills, rollback, response loss, webhook, concurrency, old worker fencing and negative provider identities. Real reservation/payment/BUYER refund then original Phase16 verifier positive/missing-refund/wrong-amount 3/3. These test-only credentials are random process environment values and are not captured here.

Native Admin Edge UI proves Shadow/Formal rotation, PAUSED/ENFORCED preservation, OFF reenabling and real OCC conflict explicit reload. The previous native failure was normalized-error handling, now fixed. Waiting Room and long-hidden native gates remain separately documented in their capability folders. phase18-verifier.json plus negative rollback test check policy audit history, namespace transition and Redis exclusivity/TTL.

All failures in diagnostics/hardening are CLOSED. Baseline SUT is ed51447154418e05ed9e4c49728f3eb114713db9, not the evidence commit. Full limits and protocol rules are in docs/phase18_admission_overload_control.md. An optional removal of Availability's extra strict authentication was rejected by automatic approval review and was not executed; its SQL cost remains. These shared-host capability tests are not production SLA or a universal availability guarantee under arbitrary authentication floods.
