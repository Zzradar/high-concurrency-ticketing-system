Layout revision correction capability evidence. Old After 4dd48c1 is invalidated, not deployable. old-price-counterexample.log is the failing v1 assertion before production edits. Round1 HTTP failures (Date-header comparison and wrong fixture column name) and schema fixture failure (copying HELD without its ownership pointer) are CLOSED; corrected assertions preserve body, ETag, revision and stock constraints. CTest's old single-SELECT text parser was updated for the lateral snapshot SELECT while retaining all static fields and no inventory-status projection assertions. No baseline, old after, protocol or diagnostics bytes were changed.

Independent reviewer script keeps the original price mutation/restoration and assertions; only repository/output location and own Phase18 transport endpoints change. The result records changed body, changed v2 validator, and HTTP 200 for old validator. Source field mapping and concurrency/migration semantics are in docs/phase18_layout_revision.md. These are capability tests, not formal A/B; complete new after-v2 collection remains required.
