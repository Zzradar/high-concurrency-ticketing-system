Final revision regression evidence, separate from OFF A/B. Production source is 6807a01516a75cf834ca1fac46fb1ac9abcdc53e. The empty-change guard in migration015 was retested with the complete OFF/HTTP/Lua/payment/refund/crash suites. Original independent price assertions remain intact. Actual ADMIN publication of 5000 and 10000 seats, each across two Sessions, writes exactly two revision rows per publication (one per Session).

CLOSED diagnostics: empty projected changes previously executed a zero-row revision UPDATE; final guard now bypasses it entirely. Docker default address pools were exhausted; financial regressions reused an explicitly owned empty network, later retired after all endpoints stopped. No other stage resources or global Docker configuration changed. A shared three-API Redis fault fixture produced one 503; original Auth/Zone assertions pass 5/5 with the intended single probe API. The peer fixture could skip a leave before mode propagation; it now waits for actual ADMITTED before DELETE and retains the original four-remaining-position assertion. Both negative outputs and subsequent passes are retained. unused-precollection/ contains setup only from an earlier candidate: no measurement, no formal baseline/after credit.

Production implementation was frozen before formal measurement. The later bulk-publication test and derived-analysis/evidence tests add no production changes. These results are not production SLA.
