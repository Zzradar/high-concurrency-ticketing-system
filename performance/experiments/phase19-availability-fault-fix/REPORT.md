Phase19 唯一阻断修复已完成，生产修复共 1 轮。修复提交 `98e3d2aa22d6015664bb995708c23800f1e1a456`；新后端二进制 SHA-256 `0db6f0c3850ac0c91e40a271dc9ca60f021fda315e7c80a9b2fcb427c67f067a`。本目录是新增证据，原 Phase19 及登录竞态修复证据保持不变。

根因是准入运行时混用了 SQL 策略失效与 Redis 调度不可用状态。调度发布 `ZADD` 异常、调度读取异常及失败结果会把全局 `dirty` 置为真，导致无关 OFF 场次返回 `ADMISSION_UNAVAILABLE`。原第 63 行的实际错误不是 `SYSTEM_OVERLOADED`；故障前 Availability 在途数、计算活跃数及队列均为 0，初始化锁和 Outbox 为 0。进程崩溃后已重新启动，旧进程内许可不会跨进程保留。单飞由 Redis 初始化锁协调。降级读取中的 Redis 持有者叠加失败有错误回调收尾，正式座位状态仍来自 PostgreSQL；本次失败请求在准入检查处已被拒绝，未进入降级读取。

旧二进制全新环境首次加连续三轮共 4/4 通过；准确前置顺序运行也通过。首个引入非 OFF 策略的前置模块是 `phase18_public_contract_test`，这属于合法业务数据，不是应删除的脏数据。补足 16 个非 OFF 策略后，完整序列 3 轮中 1 轮在原第 63 行失败、2 轮通过。新增两项 Redis ACL 持续故障反例均在旧二进制上以实际 OFF 读取 503 失败，确认了两个异常分支，消除了依赖偶然时序的验证方式。所有失败日志均保留。

修改仅涉及 `AdmissionRuntime.cpp`、新增依赖隔离测试及加强原 32 请求饱和测试。SQL 策略更新仍递增原失效序号，刷新只能在序号未变时发布；SQL 刷新失败仍使策略缓存不可用。Redis 故障只设置独立重试标志并撤销 Redis 资格就绪状态，不再使权威 OFF 策略失效。未修改 Bulkhead 容量、限流规则、原 Redis 中断 200 断言或合法 503 语义。

定向验收：新增反例 2/2；冷读、投影器退出 86、初始化器退出 87、Redis 中断降级 200、恢复及回滚组成的完整序列连续 3/3 通过。32 请求严格得到 16 个 200、16 个 `SYSTEM_OVERLOADED` 503，最大在途 16，压力解除后在途归零且读取 200。最终核对 5000 座位 SQL/Redis 状态及正式版本完全一致，Snapshot/Delta 均 200，初始化锁、Outbox 和策略审计异常全为 0，核对时有 16 个有效非 OFF 策略。

完整门禁：原后端集成 **166/166**；CTest **36/36**；完整 Vitest **344/344**；Python **323/323**；Node 协议 **2/2**；前端 production build 通过。Waiting Room、Token Bucket、Bulkhead、支付、崩溃窗口、退款、多实例及过载恢复均包含在这些真实回归中。金融夹具最初因 Docker 自动地址池耗尽未启动，使用经核对无冲突的新显式子网后继续完成，旧启动失败日志另存。新增反例调试中修正了健康端点等待条件，以及 ZRANGEBYSCORE 同时影响读模型时必须期待 degraded=true 的预期；OFF 读取 200 核心断言始终不变。

生产二进制已改变，因此以下全部使用新 SHA 重跑。发生器沿用冻结且已资格通过的 2 CPU/4 GiB 身份，事件策略 OFF、本地 Bulkhead 保持默认。Open 按 precheck → L1 → L2 → L3 原递增顺序执行。503 不计为成功吞吐。L3 首次出现 8 次真实 Availability Bulkhead 503，最大在途 16，原门禁正确中止；中止时 20 项临时展示差异随后经原验证器确认归零。没有修改保护或阈值，第二次以相同二进制、配置和负载口径通过。首次失败及原始指标单独保留在 diagnostics/capacity/open-l3-r1；本机该负载存在重复运行波动，不能把单次通过描述为每次都保证无过载。

| 场景 | 完成业务迭代 | HTTP 总数 | 观察窗 Delta/s | 观察窗写请求/s | 结果 |
|---|---:|---:|---:|---:|---|
| closed-2000-10m | 49522 | 49522 | 72.207 | 0.000 | PASS |
| open-l3-r2 | 62347 | 65988 | 500.033 | 42.850 | PASS |
| journeys-5001 | 5001 | 21955 | 0.000 | 11.200 | PASS |
| hotspot-1000 | 1000 | 1000 | 0.000 | 33.333 | PASS |

2000 VU 观察窗为完整 600 秒，另有 30 秒预热。业务场景完成 5001 次；单座位热点为 1000 VU、恰好 1 个胜者，其余冲突作为预期业务结果单独记录。各点原始结果、请求状态分类、资源样本和前后 SQL/Redis 不变量见 `capacity/` 与 `capacity-summary.json`。正式版本变化、投影及流计数保持一致。完整原始 k6 gzip 数据保留于本机私有点目录，原始 SHA 与大小写入各点归档清单；未将私有凭证或请求体打包。

`summary.json` 逐项列出 166 项计数及源提交绑定；`Manifest.json` 列出本目录文件 SHA-256。Manifest 不包含自身，避免自引用。冻结协议里的 `baseCommit` 表示 Phase18 祖先，实际执行身份以本轮 `sourceCommit` 和 `binarySha256` 为准。最终证据提交与 Git ahead/behind 状态由交付时的 Git 检查确认；未 merge、未创建 PR、未 force push。
